<input
        id="{{id}}"
        placeholder="{{placeholder}}"
        class="{{#if className}} {{className}} {{else}} input {{/if}} {{#if full}} input-full {{/if}}"
        type="{{type}}"
        ref="input"
        {{#if value}}value={{value}}{{/if}}
        name={{name}}
></input>
